#include <stdio.h>
#include "editor.h"


void open_file(const char* filename) {
printf("[EDITOR] Opening file: %s\n", filename);
}


void save_file(const char* filename) {
printf("[EDITOR] Saving file: %s\n", filename);
}